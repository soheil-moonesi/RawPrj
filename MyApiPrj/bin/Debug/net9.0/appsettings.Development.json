{
  "Logging": {
    "LogLevel": {
      //Default: Sets the baseline log level for every logger in your application that doesn't have a more specific rule.
      "Default": "Trace",
      //Sets a more specific log level for all loggers that fall under the "Microsoft.AspNetCore" category (and its sub-categories). 
      // This overrides the Default rule for those specific loggers.
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
